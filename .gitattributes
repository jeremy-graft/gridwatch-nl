# Normalise everything to LF in the repo. The archive's value is clean diffs; without this,
# Windows (local) and Linux (GitHub Actions) commits would flip CRLF<->LF and every snapshot
# would look like a full-file change.
* text=auto eol=lf
*.pbf binary
